fastapi
uvicorn[standard]
pyvmomi
pandas
openpyxl
jinja2
python-dotenv
pydantic
pydantic-settings
htmx-flask  # Note: Actually using FastAPI, so standard HTMX via templates is fine, but jinja2 is needed.
requests
pytest
